apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: mlflow
  namespace: mlflow
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`${MLFLOW_HOST}`)
      kind: Rule
      services:
        - name: mlflow
          port: 5000